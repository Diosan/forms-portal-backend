

'use strict';
import { DataTypes } from 'sequelize';

// Dynamic import for ESM model
const UserModelPromise = import('../models/index.js'); // Make sure the path is correct

module.exports = {
  async up (queryInterface, Sequelize) {
    await queryInterface.addColumn('users', 'verifierId', {
      type: DataTypes.UUID,
      allowNull: true
    },)
  },
  async down(queryInterface, Sequelize) {
    // Your down migration logic
  }
};

