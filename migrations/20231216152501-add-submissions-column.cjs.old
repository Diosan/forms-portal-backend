'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.addColumn(
      'submissions', // Name of the table
      'additionalNotes', // Name of the new column
      {
        type: Sequelize.TEXT, // Use Sequelize.TEXT for LONGTEXT data type
        allowNull: true, // Column can be null
      }
    );
  },

  async down(queryInterface, Sequelize) {
    // Remove the column if needed during rollback
    await queryInterface.removeColumn('submissions', 'additionalNotes');
  }
};
